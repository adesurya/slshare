<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delete Product - MOVA Admin</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      padding: 20px 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .card {
      border: none;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      margin-bottom: 30px;
    }
    .card-header {
      background-color: #fff;
      border-bottom: 1px solid rgba(0,0,0,.125);
      padding: 1.25rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-top-left-radius: 10px !important;
      border-top-right-radius: 10px !important;
    }
    .delete-warning {
      background-color: #fff5f5;
      border-left: 4px solid #dc3545;
      padding: 1rem;
      margin-bottom: 1.5rem;
      border-radius: 4px;
    }
    .product-img {
      width: 100px;
      height: 100px;
      object-fit: cover;
      border-radius: 8px;
      border: 1px solid #dee2e6;
    }
    .product-info {
      background-color: #f8f9fa;
      padding: 1.5rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
    }
    .product-name {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }
    .btn-danger {
      background-color: #dc3545;
      border-color: #dc3545;
    }
    .btn-danger:hover {
      background-color: #c82333;
      border-color: #bd2130;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header">
            <h4 class="mb-0">Delete Product</h4>
            <div>
              <a href="/admin/products" class="btn btn-sm btn-outline-secondary">
                <i class="fas fa-arrow-left"></i> Back to Products
              </a>
            </div>
          </div>
          <div class="card-body">
            <div class="delete-warning">
              <h5><i class="fas fa-exclamation-triangle text-danger me-2"></i> Warning</h5>
              <p class="mb-0">You are about to delete this product. This action cannot be undone. Are you sure you want to continue?</p>
            </div>
            
            <div class="product-info">
              <div class="row align-items-center">
                <div class="col-auto">
                  <img src="<%= product.image_url %>" alt="<%= product.name %>" class="product-img">
                </div>
                <div class="col">
                  <div class="product-name"><%= product.name %></div>
                  <div class="text-muted">Brand: <%= product.brand %></div>
                  <div class="text-muted">Category: <%= product.category %></div>
                  <div class="text-muted">Price: Rp<%= new Intl.NumberFormat('id-ID').format(product.price) %></div>
                </div>
              </div>
            </div>
            
            <form action="/admin/products/<%= product.id %>/delete" method="POST">
              <div class="mb-3">
                <div class="form-check">
                  <input class="form-check-input" type="checkbox" id="confirm" name="confirm" value="yes" required>
                  <label class="form-check-label" for="confirm">
                    Yes, I want to permanently delete this product
                  </label>
                </div>
              </div>
              
              <div class="d-flex justify-content-end">
                <a href="/admin/products/<%= product.id %>" class="btn btn-light me-2">Cancel</a>
                <button type="submit" class="btn btn-danger">
                  <i class="fas fa-trash me-1"></i> Delete Product
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>